// Optional: pre-fill your Apps Script Web App connection so visitors
// don't have to paste it in manually. Leave blank to configure via the
// "Connection settings" button in the UI instead (saved to that
// browser's localStorage only).
//
// If you fill these in, ANYONE who visits your GitHub Pages site will
// be able to use this API key to read/write your sheet — only do this
// if that's what you want (e.g. a private/internal tool), and prefer
// the in-app settings dialog for anything else.
window.DEFAULT_CONFIG = {
  webAppUrl: '',
  apiKey: ''
};
